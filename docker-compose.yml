version: '3.8'
services:
  postgres:
    image: postgres:latest
    container_name: postgres_container
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: meta_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:latest
    container_name: redis_container
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  mongodb:
    image: mongo:6.0
    container_name: mongodb_container
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: admin
      MONGO_INITDB_DATABASE: meta_db
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  rabbitmq:
    image: rabbitmq:3.12-management
    container_name: rabbitmq_container
    restart: always
    environment:
      RABBITMQ_DEFAULT_USER: admin
      RABBITMQ_DEFAULT_PASS: admin
    ports:
      - "5672:5672"
      - "15672:15672" # RabbitMQ 관리 UI
      - "61613:61613" # STOMP 프로토콜
    command: >
      sh -c "rabbitmq-plugins enable rabbitmq_stomp rabbitmq_web_stomp &&
             rabbitmq-server"
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq

## Mac M2칩
#  selenium:
#    image: seleniarm/standalone-chromium:latest
#    container_name: selenium
#    ports:
#      - "4444:4444"
#    shm_size: 2g

# Window x86_64
  selenium:
    image: selenium/standalone-chrome:latest
    container_name: selenium
    ports:
      - "4444:4444"
    shm_size: 2g

volumes:
  postgres_data:
  redis_data:
  mongo_data:
  rabbitmq_data:
  selenium:



# prod

#version: '3.8'
#services:
#  redis:
#    image: redis:latest
#    container_name: redis_container
#    ports:
#      - "6379:6379"
#    volumes:
#      - redis-data:/data
#    command: redis-server --appendonly yes --replicaof no one
#    healthcheck:
#      test: [ "CMD", "redis-cli", "ping" ]
#      interval: 5s
#      timeout: 3s
#      retries: 5
#      start_period: 3s
#    networks:
#      - ec2-user_default
#
#  selenium:
#    image: selenium/standalone-chrome:latest
#    container_name: selenium
#    ports:
#      - "4444:4444"  # WebDriver remote port
#    shm_size: 2g  # 크롬이 Out of memory 안 나도록 공유 메모리 증가
#    networks:
#      - ec2-user_default
#
#  app:
#    image: junhui9789/letzgo-app:latest
#    container_name: letzgo-app
#    ports:
#      - "8080:8080"
#    env_file:
#      - ./env/.env
#    environment:
#      - SPRING_PROFILES_ACTIVE=prod
#      - SELENIUM_REMOTE_URL=http://selenium:4444/wd/hub  # 중요한 부분
#      - DOTENV_PATH=/app/env
#    volumes:
#      - ./env:/app/env
#    depends_on:
#      redis:
#        condition: service_healthy  # Redis가 정상 작동할 때까지 대기
#      selenium:
#        condition: service_started
#    networks:
#      - ec2-user_default
#      - proxy-network
#
#  nginx-proxy-manager:
#    image: 'jc21/nginx-proxy-manager:latest'
#    container_name: nginx-proxy-manager
#    restart: unless-stopped
#    ports:
#      - '80:80'    # HTTP 포트
#      - '443:443'  # HTTPS 포트
#      - '81:81'    # 관리자 웹 인터페이스 포트
#    volumes:
#      - ./data:/data
#      - ./letsencrypt:/etc/letsencrypt
#    networks:
#      - proxy-network
#
#volumes:
#  redis-data:
#
#networks:
#  ec2-user_default:
#    driver: bridge
#  proxy-network:
#    driver: bridge
